/**
 * Application constants for Auth0 MCP Server
 */

/**
 * Application identifier used for debug namespaces
 * @type {string}
 */
export const APP_ID = 'auth0-mcp';

// Re-export keychain constants for backward compatibility
export { KEYCHAIN_SERVICE_NAME, KeychainItem, ALL_KEYCHAIN_ITEMS } from './keychain.js';
